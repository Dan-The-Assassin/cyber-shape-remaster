namespace Constants
{
    // Always make sure this enum has the scenes in the same order as they are in the build settings.
    public enum Scene
    {
        MainMenuScene,
        PlayMenu,
        LevelSelector,
        OptionsMenu,
        GameOverMenu,
        Level1,
        Level2,
        Level3,
        Level4,
        Level5
    }
}